# Auto-provision the Prometheus datasource for Grafana.
# This file is read by Grafana at startup; no UI clicks required.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 5s
